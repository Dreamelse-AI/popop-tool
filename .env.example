# 图像生成 API 密钥（apimart）。复制此文件为 .env 并填入真实值。
# .env 已在 .gitignore 中，不会被提交。密钥只在 vite dev server 进程内使用，不进前端 bundle。
IMAGE_API_KEY=sk-your-key-here

# 可选：图像生成 API 目标地址，默认 https://api.apimart.ai
IMAGE_API_TARGET=https://api.apimart.ai
